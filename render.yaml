services:
  # 1. Backend (FastAPI)
  - type: web
    name: strat-edge-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    rootDir: backend
    envVars:
      - key: DATABASE_URL
        sync: false # Set this to your Neon URL in Render dashboard
      - key: SECRET_KEY
        generateValue: true
      - key: GCP_PROJECT_ID
        value: custom-mix-428318-h3
      - key: GCP_BUCKET_NAME
        value: stratedgestore
      - key: GOOGLE_APPLICATION_CREDENTIALS
        value: /etc/secrets/gcp-credentials.json

  # 2. Frontend (React)
  - type: static
    name: strat-edge-portal
    runtime: node
    buildCommand: npm install && npm run build
    publishDir: dist
    rootDir: frontend
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: strat-edge-api
          property: host
